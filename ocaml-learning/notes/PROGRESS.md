# 教学进度

> 每讲完一个知识点就在这里追加。恢复对话时，从「当前位置」往下接着教。

## 当前位置

> 截至 **2026-08-16 深夜**（台式机 `ubuntu24-pc`）。**下次在笔记本上继续。**
>
> ⚠️ **2026-08-15 做过一次分支合并**：08-11 在台式机上补写的三篇概念笔记
> （`concepts/13` / `14` / `15`）当天没推上去，笔记本 08-14 又往前推了两步，
> 两边分叉。**合并时进度类文件一律以 08-14（远程）为准，只把那三篇笔记并了进来。**
> **教训：讲完就推**，不然另一台机器会重复劳动（`map`/`filter` 就被讲了两遍）。

### 🚩 路线已经单独成文，以那份为准

**[`CURRICULUM.md`](CURRICULUM.md) 是当前唯一有效的教学顺序**，包含：
排序依据（按用户锚点，不照抄教材）、各知识点对**他**的难度评估、
阶段 A–E 的完整路线、以及和两本教材（CS 3110 / ocaml.org 官方）的差异说明。

**本文件只记「讲过什么、他答对答错了什么」，不再在这里排顺序。**

### ✅ 阶段 A（只练不讲）—— 已收官

| | 练习 | 覆盖 | 状态 |
|---|---|---|---|
| **A1** | [`ex03_list_recursion`](../exercises/ex03_list_recursion/) | 列表 + 模式匹配 + 递归 + 尾递归 | ✅ **完成，11 条全过** |
| **A2** | [`ex04_record_variant`](../exercises/ex04_record_variant/) | 记录 + 变体造/拆 | ✅ **2026-08-10 完成，13 条全过** |

两道题的 `match` 骨架都已替他填好，**他只写 `->` 右边**（ex04 的 TODO 5 除外）。

**🟢 阶段 A 已结束（2026-08-10）。**

### 🔵 2026-08-10 当天又推完了阶段 B 的大半

| | 内容 | 状态 |
|---|---|---|
| B1 | **`'a` 多态** | ✅ 讲完（洞 / 同名同类型 / 不同名无约束 / **不可触碰**），笔记 [`concepts/13`](concepts/13-polymorphism.md) |
| B2 | `option` | ⏸️ **用户要求推迟** —— **根因是我拿 C# nullable 当锚，选错了** |
| B3 | **异常**（基本用法） | ✅ 讲完（**锚点用 C 的 `setjmp`/`longjmp`，别用 C# try-catch**），笔记 [`concepts/14`](concepts/14-exceptions.md) |
| B4 | **`ex05_poly_exn`** 练习 | ✅ **完成，7 题 22 条** |

**🔴 当天最重要的一条经验（已写进 `CLAUDE.md` 第 3 节）：**
用户自述「**写 C# 还停留在旧的 .NET Framework 系列语法上**，nullable 也很少用」。
→ **`record`/`with`/可空引用/模式匹配这些新特性不是锚点，是额外负担**；
.NET Framework 时代就有的（泛型 `List<T>`、`Func<>`、接口、`try-catch`、`struct`）才是。
**判断不准就退回 C。**

### 🔵 2026-08-14 推完了阶段 C 的前两步

| | 内容 | 状态 |
|---|---|---|
| C1 | `map` / `filter` | ✅ 讲完（锚点：C 的 `qsort` comparator），笔记 [`concepts/15`](concepts/15-map-filter.md) |
| C1.5 | [`ex06_map_filter`](../exercises/ex06_map_filter/) | ✅ **16 条全过，八题一次通过** |
| C2 | **`fold`** | ✅ 讲完（`fold_left` / `fold_right`）。⚠️ **还没有概念笔记，下次顺手补 `16-fold.md`** |
| C2.5 | [`ex07_fold`](../exercises/ex07_fold/) | ✅ **19 条全过**（`? :` 已改成 `if … then … else`） |

**C2 的有效讲法（务必复用）**：从**他自己在 ex03 第 4 题手写的 `go acc rest`** 切进去——
「这段代码里只有两处和求和有关：`acc + x` 和 `0`，`fold` 就是把这两个旋钮做成参数」。
**路线里预判他会卡在 fold，结果没卡**，反而自己推出了 `fold_right` 的语义和代价。

### 🔵 2026-08-16 讲完 C3 + C3.5，并开始扫 D1

| | 内容 | 状态 |
|---|---|---|
| C3 + C3.5 | `\|>` / `@@` + **运算符全表** | ✅ 笔记 [`concepts/17`](concepts/17-operators.md) |
| D1（部分） | **`function`** + **嵌套模式** | ✅ 笔记 [`concepts/18`](concepts/18-function-and-patterns.md) |
| — | **`List` 模块速查** | ✅ 笔记 [`concepts/19`](concepts/19-list-stdlib.md) |
| — | 📝 **`ex08_list_stdlib`**（8 题 16 条） | ⬜ **已备好，一题未写** |

**⛔ 下一步**：先做 `ex08_list_stdlib`，再扫 D1 剩下的
**`when` 守卫 / `and` 相互递归 / 可变记录 `<-` / 数组**。

**⚠️ 仍然欠一篇 `16-fold.md`**（C2 的概念笔记，08-14 讲的，一直没补）。

**🔴 长期有效**（阶段 A 就是为它排的，现在依然管用）—— 用户 2026-08-08 原话：

> 「最好不要跳过太多的基础内容。先按照建议的 ocaml 语言上手顺序巩固好已经学过的内容，再开始新内容。」
> 「安排之前反复讨论过但是没有练习的部分先进行一些练习。」

→ **每个新知识点讲完都要立刻配练习，并且当天就推上去。**

### 2026-08-08 这一天讲了什么

**全天由用户提问驱动，讲了三大块，但他一行代码都没写**——
这正是 2026-08-06 复习轮暴露的老问题（讲过没练过 = 留不住），所以下一步排的是纯练习。

| 内容 | 笔记 |
|---|---|
| **记录（record）** | [`concepts/11-records.md`](concepts/11-records.md) |
| **列表** | [`concepts/12-lists.md`](concepts/12-lists.md) |
| **`match` 从头补强（一大节）** | [`concepts/09`](concepts/09-adt-and-pattern-matching.md) 的 **9.8** |

**9.8 是这一天最有价值的产出**，起因是他说「我对模式匹配的了解很有限」「拆列表这块在我的逻辑里
根本讲不通」。**诊断出的根因是我的锅**：列表模式同时干了①分辨形状和②取出数据两件事，
而他两件都没单独见过，我直接上了叠加态。**有效的讲法（务必复用）**：
先用常量讲①→再用元组讲②→最后回列表看叠加。

**ADT 的暂停已解除**，而且他最后**自己给出了正确总结**（详见 `MASTERY.md` 同日）。

### 🔧 2026-08-08 晚的两处环境改动（都已实测）

1. **OCaml 文件里关掉了 AI 补全**。起因：**Codeium 根据练习题注释里的功能描述，
   直接补全出了正确实现**，答案送到眼前，练习白做。
   做法在 `.vscode/settings.json`：`codeium.enableConfig` 里只把 `ocaml` /
   `ocaml.interface` 设为 false，**其他语言不受影响**；
   **OCaml LSP 自己的类型补全保留**（那个只提示类型，不写算法，对学习有益）。
   ⚠️ 该文件是 `.gitignore` 的特例（`**/.vscode/*` + `!**/.vscode/settings.json`），
   **两个 gitignore 坑记在 `docs/env/COMMON.md`**，别改回去。
2. **`exercises/dune` 加了 `-39`**（unused rec flag）。递归题的骨架里函数体还是
   `failwith`，`let rec` 的 `rec` 会报警，而那个 `rec` 正是题目要他填的东西，先报出来纯干扰。

### ⚠️ 这一天我欠的一个纠正（已当面说明，别忘）

我一直说「递归代替循环」，可能让他以为 **OCaml 没有循环**。
实际有 `for` / `while`（实测通过），只是不能 `break`、循环体必须是 `unit`、想攒结果得配 `ref`。
**社区不用循环不是因为没有，是因为循环在不可变的世界里不好用。**

### ⚠️ 长期方针（仍然有效）

- **2026-08-05 深夜**：「先不啃语言哲学，先学社区推荐的常用写法」→ 语法边角只在挡路时讲，
  **收网是我的责任，别等他喊停**（已经喊过六次）。
- **2026-08-07**：C# 类比**放开用**（他明确想加深 C#），但 C 类比在它擅长的领域仍然更强。
- **给选择题，别给提醒**（ex01 破局、08-06 复习轮、08-08 排路线，三次验证有效）。

---

## 已讲知识点

### 1. `let` 是绑定，不是变量声明

- 形式上像 C 的 `int x = 5;`，但有两点不同：
  - **类型不用写**，编译器自己推出来；而且是**编译期就定死**的，不是运行时随便。
    （提醒过：这不同于 C# 的 `var`/C 的 `auto` 那种印象，虽然 `var` 其实也是编译期推导，
    但用户是初学者，重点是让他知道「省略类型 ≠ 动态类型」。）
  - 它是**绑定**不是变量，绑定之后不能改。
- 用到的对比：C 的 `int x = 5;`。
- 状态：已讲，**用户尚未反馈是否理解**。

### 2. 整数和浮点：两套运算符，没有隐式转换（进行中）

- 要点：`+ - * /` 只用于 `int`；浮点是另一套 `+. -. *. /.`；
  两者之间**不会自动转换**，要显式 `float_of_int` / `int_of_float`。
- 用到的对比：C 里 `5 + 1.5` 会把 `5` 悄悄提升成 `double`，OCaml 宁可编译不过。
- 状态：**已出练习 `ex01_int_float`，用户还没写。**

### 3. `let … in` 的作用域，以及同名遮蔽

**详细笔记见 [`concepts/03-let-in-scope.md`](concepts/03-let-in-scope.md)。**
注意这一点是**提前讲的**——原计划排在函数定义之后，因为用户从公开课带问题回来，顺势就讲了。
知识点 2 的练习到现在还没写。

- 起因：用户在公开课上看到讲师演示作用域，抄回来两个式子问是什么意思。
  **他抄漏了 `in`**（`let x = 5 in (let x = 6)`），正好借语法报错切入。
- 讲了三件事：① `let … in <body>` 是不可分割的整体；
  ② body **贪婪吃到右边尽头**，所以 `in` 后面不用括号，括号是用来**截断**它的；
  ③ 同名绑定遮蔽的是**名字**不是值，外层绑定从未被修改。
- 用到的对比：C 的块作用域 `{ int x = 5; … }`；连续 `let … in` 对应 C 的连续声明语句，
  但 OCaml 那一整坨是**一个表达式**。
- 白送的教具：`Warning 26 [unused-var]` 正好指向被遮蔽的外层 `x`，是遮蔽发生的直接证据。
- 状态：**已讲，用户反应良好**（见 MASTERY）。留的口算题 2026-08-05 答对了（21）。

### 4. 函数是值：lambda、应用、部分应用、多参数的真相

**详细笔记见 [`concepts/04-functions-as-values.md`](concepts/04-functions-as-values.md)。**
又是**提前讲的**——用户从公开课带回 `(fun x -> x + 1) (2 + 3)` 问这是什么。

- 核心：`fun x -> …` 本身就是一个值，和 int 完全平级（能对 int 做的事都能对函数做）。
- `let f x y = …` 是 `let f = fun x -> fun y -> …` 的语法糖；**OCaml 里没有多参数函数**，
  `f a b` 就是 `(f a) b`，一次吃一个。
- 三条方向规则：`fun` 箭头右结合、类型箭头右结合、**函数应用左结合**。
- 优先级两极：**函数应用最高**，`fun ->` / `let … in` 最低。
- 求值顺序两件事分开：**算参数右→左（unspecified！）**，**吃参数左→右**。
- `let x = e1 in e2` ≡ `(fun x -> e2) e1` —— 编译器里的脱糖，写 toylang 会用上。
- 用到的对比：C 的宏 vs C 函数（用户提出的，洞察对但对应错位置——
  OCaml 传值不传表达式，副作用恰好一次）。
- 状态：**已讲透，用户掌握良好。**
- ✅ **2026-08-05 补记：「柯里化」这个名字已经给出去了，这一点闭环。**
  切入点是公开课代码里的 `String.concat "\n"` —— 我问「只给第一个参数会得到什么」，
  用户答「**它会变成一个期待输入字符串列表且返回字符串的另一个函数**」，**完全正确**，
  当场贴标签。同时把两个词分清楚了：
  **柯里化 = 语言的设计**（`a -> b -> c` 本来就是一串单参数函数）；
  **部分应用 = 你的用法**（只喂一半就停手）。

### 5. 类型转换：只有显式，转换就是普通函数

**详细笔记见 [`concepts/05-type-conversion.md`](concepts/05-type-conversion.md)。**
这是**知识点 2 的实用补完**，用户问「是不是既没有隐式转换也没有强制转换」。

- `float_of_int` / `int_of_float` 就是普通函数，没有转换语法。类型标注 `(5 : float)` 只检查不改变。
- ⚠️ `int_of_float` 是**朝零截断**不是向下取整，负数上和 `floor` 分道扬镳。
- ⚠️ **「带点 = 浮点版」只是命名习惯不是规则**：比较运算符 `=` `<` 是多态的（`'a -> 'a -> bool`），
  `**` 不带点却只吃 float，`mod` 不带点却只吃 int。
- ⚠️ `==` 是物理相等，`"abc" == "abc"` 是 **false**。C#/Java 背景的经典陷阱。
- 顺带点到了 `ref` / `:=` / `!`（真正的赋值），但只是让他知道存在，**不算讲过**。
- 状态：已讲。**`ex01_int_float` 需要的知识到此全齐。**

### 6. 公开课代码带出来的一串（分号 / unit / 格式串 / 注解 / 括号）

**详细笔记见 [`concepts/06-course-snippet-tour.md`](concepts/06-course-snippet-tour.md)，
条目很多，接手前务必读那一篇，否则会重复讲。**

这一批**不是我排的课**，是用户连着甩了三张伯克利公开课截图问出来的
（一个玩具编译器：无视输入 → 插入输入 → 写进文件）。清单：

| # | 点 | 一句话 |
|---|---|---|
| 6.1 | `String.concat` | `模块名.函数名`，模块系统本身没讲 |
| 6.2 | 列表用**分号**不是逗号 | ⚠️ `[1, 2, 3]` 能编译，是**一个三元组**的列表 |
| 6.3 | `;` 的两个身份 | `[ ]` 里是分隔符，`[ ]` 外是**顺序执行**（左边须为 unit） |
| 6.4 | `Printf.sprintf` | **格式串是编译期类型检查的**，`%s` 进了类型；C 里是运行时炸 |
| 6.5 | 类型注解 | 是「**断言+核对**」不是「指定」；能**收窄**多态但绝不转换 |
| 6.6 | 参数注解的括号 | **语法必需**。`let f x : int` 的注解落在**返回值**上 |
| 6.7 | `unit` | 不是 C 的 void：**真类型 + 真值 `()`**；`-> unit` ≈ 有副作用 |
| 6.8 | `(compile program)` 的括号 | 必需，**函数应用左结合**（呼应知识点 4） |
| 6.9 | `let … in` body 吃两行 | body 优先级低于 `;`（呼应知识点 3） |
| 6.10 | `program` **不是 argv** | 澄清了用户的猜测；真 argv 是 `Sys.argv`，**没有 argc** |

- **提了没展开的**（别当讲过）：`Fun.protect` / 没有 RAII、`'a` 多态、模块系统、`Array`。
- 状态：**都是随问随讲，用户没有被单独考过**，所以掌握程度**无客观依据**。

### 7. 玩具编译器的完整链路（`ignore` / `Unix` / 平台差异）

**详细笔记见 [`concepts/07-toy-compiler-pipeline.md`](concepts/07-toy-compiler-pipeline.md)。**
第 4 张截图：课上加了 `compile_and_run`，打通「源码 → 汇编 → 链接 → 跑 → 抓输出」。
**这一篇是当晚用户说「要实用」之后讲的，所以偏工程、不偏语法。**

| # | 点 | 一句话 |
|---|---|---|
| 7.1 | `ignore : 'a -> unit` | **普通函数不是关键字**；因为分号左边必须 unit 才需要它 |
| 7.2 | ⚠️ `ignore` 掩盖真错误 | `Unix.system` 返回退出码，吞了 = 不检查 `system()` 返回值 |
| 7.3 | `Unix` 是**单独的库** | dune 要写 `(libraries unix)`，否则 `Unbound module Unix` |
| 7.4 | `close_in inp; r` | `e1; e2` 取 e2 → 这就是它返回 string 的原因 |
| 7.5 | ⚠️ `macho64` 是 **macOS** 的 | 讲师在 Mac；`ubuntu24-pc` 上要改 `elf64` |
| 7.6 | `runtime.o` | 截图里没给的那一半：C 写的运行时，提供 `main` 调 `_entry` 并打印 |

- **7.2 有个现场**：我改汇编时手滑导致链接失败，`ignore` 吞掉错误，
  程序继续跑 `./program` 最后炸在 `End_of_file` —— **报错点离病根隔了两步**。
  用「不检查 `system()` 返回值」跟他类比，C/内核背景的人秒懂。
- **实测跑通了**（`compile_and_run "5000" => 5000`），完整配方在 concepts/07，
  照抄即可重建（原代码在 scratchpad，已随 session 消失）。
- 状态：**同样是随问随讲，没考过他**，无掌握程度依据。

---

### 8. 不可变性与 `ref`

**详细笔记见 [`concepts/08-immutability-and-ref.md`](concepts/08-immutability-and-ref.md)。**
2026-08-06 讲的，**回到原路线后的第一个知识点**。

- 遮蔽 ≠ 赋值（三个实验）；`ref` 是**单字段可变记录**，`!`/`:=` 是记录字段读写的语法糖
- 词法作用域：**求值在运行期，名字解析在编译期**——两件事两个时间点
- `ref` + 闭包（`make_counter`）；类型构造器是后缀的
- ⭐ **他在这里提了一个很尖锐的反驳**（指出我的实验有混淆因素），见 `MASTERY.md`

### 9. 代数数据类型（ADT）与模式匹配

**详细笔记见 [`concepts/09-adt-and-pattern-matching.md`](concepts/09-adt-and-pattern-matching.md)。**
2026-08-06 开讲 → 当天被用户按暂停 → **2026-08-08 大幅补强并解除暂停**。

- 9.1–9.7（08-06）：`type` ≠ typedef、构造器 + `of`、造/拆同形、穷尽性检查、
  vs C tagged union、`match` 的两个坑、`_` 是模式
- **9.8（08-08，一大节）**：`match` 基础 vs C switch、分支类型必须一致是「它是表达式」的后果、
  **①分辨形状 / ②取出数据的分解**、用 C 摊开模式怎么拆、**位置决定方向 ≈ lvalue/rvalue**、
  模式里能写什么、**构造器留痕迹函数不留**、元组模式、`bool`/`unit` 也是 ADT、**C# 对照**
- 状态：**模型已立住**（他自己给出了正确总结），**但一行代码没写** → ex04 专治

### 10. 递归代替循环

**详细笔记见 [`concepts/10-recursion.md`](concepts/10-recursion.md)。** 2026-08-07 讲完 10.1–10.7。

- `rec` 只干一件事：**让等号右边也能看见正在定义的名字**
- 基准情形 / 递归情形；**尾递归**（判据：递归调用回来之后还有没有活干）
- 内部辅助函数 `go` / 闭包 / **词法作用域 vs 动态作用域**（他在这里答错过，用 bash 演示后立刻通）
- ⚠️ **`if` 和 `let … in` 在「后面的 `;`」上行为相反**（10.5，撞出来的）
- 状态：**讲完了，但没练过** → ex03 专治

### 11. 记录（record）

**详细笔记见 [`concepts/11-records.md`](concepts/11-records.md)。** 2026-08-08，公开课截图带出来的。

- OCaml 版 struct；和变体是一对（**或** vs **且**）；`p.itm` 取字段
- 默认不可变，`{ p with fab = … }` 是**运行时造新记录**
- **C# 9 `record` 的 `with` 表达式是同一个东西**，连关键字都一样
- `mutable` 字段用 `<-` 赋值（**只提了没展开**）

### 12. 列表

**详细笔记见 [`concepts/12-lists.md`](concepts/12-lists.md)。** 2026-08-08。
⚠️ **这个问题抛出去五次才收到回答**（每次都被他更好的问题岔开，不是拒绝）。

- `[1;2;3]` ≡ `1 :: 2 :: 3 :: []`；`::` 是「在头上接一个」不是追加
- ⭐ **本质是单向链表不是数组** → 头部 O(1)、其余 O(n) → 「`::` 攒 + `List.rev`」惯用法
- `::` **不复制**（实测物理相等），不可变性买到的实惠
- `@` 是 O(n) 且复制左半段；循环里用 n 次 = O(n²)，**最常见的性能陷阱之一**
- ⭐⭐ **列表就是一个 ADT**，`[]`/`::` 就是它的两个构造器（照抄定义能编译，编译器自己也这么叫）
- ⚠️ **只讲了「数据结构」那一半**，`map`/`filter`/`fold`/`List` 模块**整块空白**
  ← 两本教材都算它是列表的一部分，见 `CURRICULUM.md` 阶段 C

---

## 待办 / 计划中的路线

**→ 已经移到 [`CURRICULUM.md`](CURRICULUM.md)，那份是唯一有效的。**
本节原有的顺序（2026-08-06 版）已作废，别再照它走。

---

## 事件记录（不是知识点，但影响怎么读上面的内容）

### 2026-08-03 — 交接自检 + 双机改造

- 跑了 `docs/HANDOFF-CHECK.md` 的 5 个探针，**全过**，确认换位置后新对话能正确接手。
  ⚠️ **那 5 轮问答是测试，不是真实教学**：其中「`let y = x + 1.5` 能不能编译」
  和「列表 vs 数组」两个问题用户从未真的作答，不要当成他的学习表现记进 MASTERY。
- 用户说明有**两台交替使用的开发机**（Windows 笔记本 / Ubuntu 24.04 台式机），
  平台相关内容已拆到 `docs/env/`，`scripts/ocaml.sh` 改成自带平台识别，
  两台机器命令统一为 `bash ./scripts/ocaml.sh ...`。
- 用户要求练习题**全部由 Claude 预先准备好**，他只打开一个 `main.ml` 写代码。
  约定写进 `docs/EXERCISE-FORMAT.md`，`ex01_int_float` 是第一个按新格式做的例子。
- 用户要求 **git 一律等他开口**，Claude 不主动 commit/push/pull。
- 做了交接自检的自动化版本：新对话敲 `/handoff-check` 即可
  （任务书 `.claude/commands/handoff-check.md`，两层各一份，根目录那份只是转发）。
  当天跑过一次，结果和原对话的核对结论都在 `notes/handoff-report.md`，**闭卷 6/6、机械检查 5/5**。
- 自检发现并修掉的问题：`scripts/ocaml.sh check` 原先只输出 3 项版本，
  核对 `docs/env/COMMON.md` 的全部口径得绕过脚本手敲平台相关命令。现已补齐到 8 项。

### 2026-08-03 下午 — 工具环境插曲（**教学没有推进**）

用户在公开课里看到讲师用 `utop`，问那是什么。**教学进度一点没动，`ex01_int_float` 仍然一个 TODO 没写。**
接手的对话不要把下面这些当成学过的知识点。

- 讲清了 utop = OCaml 的加强版顶层（REPL），是 opam 包不是 macOS 专属，本机装在 WSL 里。
  **提前打了 `;;` 的预防针**：那是顶层的规矩，不是语言的一部分，写 `main.ml` 时不需要。
  → 记进 `docs/env/win10-laptop.md` 和 `docs/COMMANDS.md`。
- 查证了 Windows 原生 OCaml：opam 2.2 起官方支持（`winget install OCaml.opam`，底下仍要 Cygwin）。
  **结论是不装** —— 本机已有完整的 WSL switch，再装一套会有两个 switch 两套 PATH；
  且 ocaml.org 自己就推荐 WSL2。以后用户再问，直接引用这条，不用重新查。
- 实测发现 `docs/env/win10-laptop.md` 里 `wsl -d Ubuntu -- <命令>` 的写法是错的，
  不套 `bash -lc` 就没有 opam 的 PATH。**已修正**。
- 顺带把本机 PowerShell 升到 7.6.4，过程中的两个坑（winget 装的是 MSIX 不是 MSI、
  「添加删除程序」里的条目命名有误导性）已记进 `docs/env/win10-laptop.md`。跟 OCaml 无关。

**下次开工第一件事**：问用户 `ex01_int_float` 写了没有，写了就直接
`bash ./scripts/ocaml.sh run ex01_int_float`。

### 2026-08-05 — 编译器学习路线的长讨论（**不是 OCaml 教学**）

用户花了大半个对话规划**学完 OCaml 之后的路线**，涉及形式化验证、LLVM IR 生成、
C 预处理器工作量、斯坦福/哈佛/康奈尔/伯克利的课程对比、MIPS 是否已死、
以及「借助 LLM 能不能跳过没价值的问题」。

**结论全部整理进了仓库根的 `compiler-roadmap/`，不在这里重复：**

- `compiler-roadmap/ROADMAP.md` —— 路线本身，含「明确排除的方向」和「已推翻的判断」
- `compiler-roadmap/skills-profile.md` —— **用户的技能栈档案**（下次细化规划前必读）
- `compiler-roadmap/resources.md` —— 课程/书/工具清单，**每条都标了查证状态**

**跟 OCaml 教学有关的只有一条**：用户的最终目标（编译器）会影响教学侧重——
代数数据类型 + 模式匹配是重头戏，因为那是写编译器的核心武器。这一点路线里已经确认。

**这轮讨论中我犯的两个错，都已在文档里如实记录：**

1. 用循环写法查 WSL 工具链，变量被吃掉，**误报 gcc 没装**（实际装着 15.2.0）。
   教训：`for t in a b; do ... $t ...; done` 这种写法通过 `wsl -- bash -lc '...'` 传递时不可靠，
   **直接写死命令名去查**。
2. 建议「第一个后端用 x86-64 因为用户已经熟悉」，被用户用「LLM 能填补知识缺口」正确推翻。

---

## 记录格式约定

每个知识点记：讲了什么、用了什么类比、用户的反应（答对/答错/没反馈）。
用户的反应是判断掌握程度的唯一客观依据，别漏记。
