; 对 exercises/ 下所有练习生效的公共编译设置。
;
; 目的只有一个：**让写到一半的练习也能编译运行**。
;
; dune 的 dev 模式默认把很多警告升级成错误。练习骨架里函数体还是
; failwith "TODO"，参数自然没用上，于是「未使用的变量」直接变成编译错误，
; 报错内容跟题目毫无关系，很干扰学习。
;
;   -warn-error -a  所有警告都不再当作错误（真正的类型错误当然还是照报）
;   -w -26-27-32    未使用的变量/绑定/顶层定义，连警告都不打印，省得刷屏
;
; 其余警告保持 dune 的默认集合，该提醒的还会提醒。
(env
 (_
  (flags
   (:standard -warn-error -a -w -26-27-32))))
