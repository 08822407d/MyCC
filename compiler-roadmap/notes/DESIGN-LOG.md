# 设计日志

> **用途**：每一次关于「这门语言 / 这个编译器怎么设计」的对话，在这里留一条。
> **这是跨对话、跨机器传递状态的唯一载体** —— 新对话接手时读最后一条就能续上。
>
> **格式约定见 [`../docs/DOC-CONVENTIONS.md`](../docs/DOC-CONVENTIONS.md)。**
> 一条至少要有：**日期 + 机器 + 谈了什么 + 结论 + 遗留**。
> ⚠️ **用户是语音输入，转写不准。没复述确认过的内容不要当结论写进来**，标 ⬜ 待确认。
>
> **新的记在最上面**（倒序），找最近状态不用翻到底。

---

## 2026-08-21（`ubuntu24-pc`）· 项目定位交代 + 两个方向性决策 + 建目录结构

**这是本目录第一次有「设计讨论」性质的记录。** 在此之前它只有三份静态文档
（`ROADMAP.md` / `skills-profile.md` / `resources.md`，2026-08-05 ~ 08-12 写的）。

### 用户交代的项目定位（语音输入，已复述确认）

> 这个目录主要存放**最终设计成品的过程和结果文件**，
> 以后其他相同目的的工作上下文 / 对话上下文都在这里存一份。

**补充（用户当天第二条消息）**：目录用途确实是路线图 + 设计思路 + 资料收集，
**但代码不适合放这里** —— 要在 `MyCC/` 下新开一层专门放代码成果，
**和 `ocaml-learning/` 平级**。→ 已建 `../compiler/`。

### 用户复述确认的五点（他回「其他 4 点和我想的一致，你的识别没有问题」）

1. 本目录 = 设计过程 + 设计结论的文档层（**已补充：代码另开一层**）
2. 用 **OCaml** 实现编译器；**不做预处理器**，用现成的 `gcc -E`，没必要重新设计一遍
3. OCaml 已学到有基本了解，但拿它写编译器**还差点火候**
4. 切入点本来想用 **ocamllex** 快速做词法分析器，但他判断**词法理论密度低、
   学习价值不是最高，现在不急着实现**
5. 现在真正要做的是：**一段较长的问答 + 资料收集 + 想法收集 + 设计目标评估**，
   先定「这门语言要支持哪些特性」

### 当天定下的两件事

| | 问题 | 用户的选择 | 落到哪 |
|---|---|---|---|
| 1 | 目标语言的形态 | **C 子集 + 自选扩展**（以 C 为底，自己决定砍什么、加什么） | 决策 [`003`](decisions/003-source-language.md) |
| 2 | 这轮设计问答怎么推进 | **先补背景地图，再逐条决策**（先集中查证整理，有了地图再开始决策） | 本条 + `survey/README.md` |

**选项 2 的另外两个候选**（他没选，但记下来避免重提）：
「一次一个决策点、他先判断」、「我先出完整草案他再删减」。
⚠️ **注意**：他选了「先补地图」不等于放弃「他来拍板」——
`CLAUDE.md` 第 2.2 节那条（**设计决策归用户**）**始终有效**。

### 我做的事

- 读了 `skills-profile.md` / `ROADMAP.md` / `resources.md` 三份
- 照 `../ocaml-learning/` 的体例建了本目录的记录结构（本文件即其中之一）
- 新建 `../compiler/`（空壳 + README，说明动工条件）

### 遗留（→ `OPEN-QUESTIONS.md`）

- ⬜ **三处语音识别没听清，还没确认**：
  ① 「实现一个〈肺源〉这样的…」那个词没还原出来；
  ② 「我在本科去核市场的时候是写功能」——我理解成「我是用**语音输入**描述的」；
  ③ 「编译器的基础课上…我现在差一点一次」——我理解成「**差点火候**」。
  → 用户后来回「其他 4 点和我想的一致」，**间接说明②③的理解没问题**，
  但**①那个词至今没有确认**。
- ⬜ **背景地图的专题清单**（`survey/README.md`）是我拟的草案，**用户还没确认要查哪几个、顺序怎么排**
- ⚠️ **`ROADMAP.md` 阶段 1 的表述已经和决策 003 不一致**（那里写的是「用书把范围钉死」），
  当天只在阶段 1 加了指向决策 003 的说明，**正文没重写** —— 等特性集收敛后统一改
