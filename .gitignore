# dune / OCaml
_build/
*.install
*.merlin
.merlin
*.cm[ioxa]
*.cmx[as]
*.o
*.a
*.so
*.exe
*.annot

# opam 本地 switch（如果以后在仓库里建了 _opam）
_opam/

# Claude Code 的个人级覆盖设置（因机器而异，不进仓库）
.claude/settings.local.json

# Claude 自己验证用的临时代码，按约定可随时清空，不进仓库
# （见 ocaml-learning/CLAUDE.md 第 7 节的目录约定）
scratch/

# 编辑器 / 系统
# .vscode/ 整体不进仓库，但 settings.json 是个例外 —— 它记录的是
# 「OCaml 练习时关掉 AI 补全」这条教学设定，两台机器都需要，所以要同步。
# 两个坑（都实测过）：
#   1. 目录被整体 ignore 时 git 不会下探，负向规则会失效 → 必须写成 .../* 再放行
#   2. 含有内部斜杠的模式会被**锚定到仓库根**，所以 .vscode/* 只能匹配根目录那个；
#      要匹配任意层级必须写 **/.vscode/*
**/.vscode/*
!**/.vscode/settings.json
.DS_Store
Thumbs.db
*~
*.swp
