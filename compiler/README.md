# compiler — 编译器本体（代码）

> **状态：⬜ 空的，还没动工。这是有意的，不是漏了。**
>
> 2026-08-21 建立。用户当天明确说：设计资料那个目录不适合放代码，
> **要在 `MyCC/` 下单开一层专门放代码成果，和 `ocaml-learning/` 平级** —— 就是这里。

## 分工

| 目录 | 放什么 |
|---|---|
| **`../compiler-roadmap/`** | **设计过程 + 设计结论**（路线、决策、资料调研、语言规范定稿） |
| `../compiler/`（本目录） | **编译器源码**、构建脚本、测试 |
| `../ocaml-learning/` | OCaml 语言学习（**另一条线，别混**） |

**要知道这个编译器打算长成什么样，看 [`../compiler-roadmap/CLAUDE.md`](../compiler-roadmap/CLAUDE.md)。**

## 已经定下的（详见 `../compiler-roadmap/notes/decisions/`）

| | |
|---|---|
| 实现语言 | **OCaml**（决策 001） |
| 源语言 | **C 子集 + 自选扩展**（决策 003）—— 具体特性⬜**一条都还没定** |
| 预处理器 | **不实现**。编译器吃 `gcc -E` 产出的 `.i` 文件（决策 002） |
| 后端目标 | ⚠️ **暂定**输出 LLVM IR 文本（决策 004，**用户没正面确认过**） |
| 词法/语法工具 | `ocamllex` / `menhir`（两台机器都已装） |

## 动工条件（**满足之前不要在这里写代码**）

1. **特性集收敛** —— `../compiler-roadmap/notes/feature-inventory.md` 至少把
   「第一个能跑通的最小子集」定下来。**token 集是特性集的直接产物**，
   特性没定，词法分析器写了也是白写
2. **后端目标复议** —— 决策 004 现在是 ⚠️ 暂定
3. **LLVM 工具链装上**（如果维持决策 004）—— 2026-08-05 实测两台机器都没装
4. **OCaml 够用** —— 用户 2026-08-21 自述「拿它写编译器还差点火候」。
   判据还没定（`OPEN-QUESTIONS.md` 的 Q6）

## 动工时要建的东西（**到时候再建，现在别提前**）

`dune-project` / `bin/` / `lib/` / `test/` / 构建脚本 / 本层的 `.claude/settings.json`
（照 `../ocaml-learning/.claude/settings.json` 的样子写，路径相对本目录）。

⚠️ **别照搬 `ocaml-learning/exercises/` 的结构** —— 那是教学练习的组织方式，
一道题一个子目录，和一个真实项目的布局不是一回事。
